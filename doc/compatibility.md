## C++ library 应该如何发布

### 动态库和静态库的缺陷
动态库的部署、运维、升级需要严格的管理，不同版本的所有组合都需要测试，二进制兼容性问题很难解决。动态库适合系统库，或者绝对要被大量应用使用的非常基础的库。一般的业务库应该避免动态库。

静态库管理起来同样麻烦，命名时要加上它依赖的gcc版本、各种底层库版本、是否用了c++11的版本。不同版本不能混用。

### libasync的发布方式
最方便简单健壮的方法是源码编译+静态链接+cmake。把整个项目拷贝过去，cmake加一个add_subdirectory就可以使用了。